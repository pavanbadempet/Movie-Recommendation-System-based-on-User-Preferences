APEX Recommendation API {{ .Chart.AppVersion }} deployed successfully.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Serving tier: {{ .Values.servingTier | upper }}
  tier1 → GPU + full 6-model PyTorch ensemble + online learning
  tier2 → ONNX CPU quantized inference (current)
  tier3 → FAISS + TF-IDF only (low-memory)

DP epsilon: {{ .Values.dpEpsilon }} (Gaussian ε-DP on user embeddings)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Access the API:

{{- if .Values.ingress.enabled }}
  External: https://{{ .Values.ingress.host }}/docs
{{- else }}
  Port-forward to reach the API locally:

  kubectl port-forward svc/{{ .Release.Name }}-apex {{ .Values.service.port }}:{{ .Values.service.port }} -n {{ .Release.Namespace }}

  Then open: http://localhost:{{ .Values.service.port }}/docs
{{- end }}

Health check:    http://localhost:{{ .Values.service.port }}/health
SLO report:      http://localhost:{{ .Values.service.port }}/v1/platform/slo
Metrics:         http://localhost:{{ .Values.service.port }}/metrics

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scaling:
  Replicas: {{ .Values.replicaCount }}
{{- if .Values.autoscaling.enabled }}
  HPA: {{ .Values.autoscaling.minReplicas }}–{{ .Values.autoscaling.maxReplicas }} replicas @ {{ .Values.autoscaling.targetCPUUtilizationPercentage }}% CPU
{{- end }}
{{- if .Values.podDisruptionBudget.enabled }}
  PDB: minAvailable={{ .Values.podDisruptionBudget.minAvailable }} (safe rolling updates)
{{- end }}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

To upgrade serving tier to Tier1 (GPU):

  helm upgrade {{ .Release.Name }} . \
    --set servingTier=tier1 \
    --set resources.requests.memory=16Gi \
    --set resources.limits.memory=24Gi

To enable Prometheus ServiceMonitor:

  helm upgrade {{ .Release.Name }} . \
    --set serviceMonitor.enabled=true \
    --set serviceMonitor.namespace=monitoring

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

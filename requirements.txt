# Movie Recommendation System - Dependencies
# Pinned to exact versions for reproducible builds.
# To upgrade: pip install --upgrade <package> && pip freeze | grep <package>

# Core data processing & Apache Spark 4.x
pandas==2.2.3
numpy==2.4.4
pyarrow==23.0.1  # Parquet support — compatible with NumPy 2.x
polars==1.41.2   # Fast Rust-backed DataFrame library for optimized ingestion
pyspark>=4.0.0   # Apache Spark 4.x distributed compute engine
delta-spark>=4.0.0 # Delta Lake 4.x ACID storage engine

# Machine learning & NLP
scikit-learn==1.8.0
sentence-transformers==5.4.1  # Neural embeddings (SBERT)
faiss-cpu==1.13.2             # Similarity search (swap for faiss-gpu on GPU hosts)
yt-dlp>=2025.2.19             # On-demand video download
turbovec==0.7.0                  # Fast quantized vector search index
onnx==1.22.0                  # Model export
onnxruntime==1.26.0           # Lightning-fast CPU inference
transformers==5.10.0           # CLIP Vision Encoding
Pillow==12.3.0                # Image processing

# Data validation
pandera==0.31.1

# Web backend & High-Speed Protocols
fastapi==0.136.1
starlette==1.3.1               # Security fixes for CVE-2026-54282/CVE-2026-54283
uvicorn[standard]==0.46.0
pydantic==2.13.3
slowapi==0.1.9                # Rate limiting
grpcio>=1.60.0                 # High-throughput gRPC framework
grpcio-tools>=1.60.0           # Protobuf compiler for gRPC stubs
h2>=4.1.0                      # HTTP/2 protocol support for Uvicorn/gRPC

# Frontend
streamlit==1.57.0
st-clickable-images==0.0.3    # Clickable image grid
plotly==6.7.0                 # Frontend charts
requests==2.33.1

# Utilities
python-dotenv==1.2.2
joblib==1.5.3
sentry-sdk[fastapi,pure_eval]==2.58.0  # Error Monitoring
httpx==0.28.1                          # Async HTTP client
psycopg[binary]==3.3.4                 # Durable Postgres driver
sqlalchemy==2.0.49                     # ORM and Connection Pooling
python-jose[cryptography]==3.5.0       # JWT auth
cryptography==50.0.0                   # Security fix for GHSA-537c-gmf6-5ccf
passlib[bcrypt]==1.7.4                 # Password hashing
bcrypt==4.0.1                          # Pin to avoid passlib compatibility issue
prometheus-client==0.25.0             # Observability metrics
psutil==7.2.2                          # Hardware detection (RAM, CPU cores)

# Optional performance dependencies
orjson==3.11.8  # Fast JSON serialization; falls back to stdlib json if absent
rustworkx==0.16.0  # Fast Rust-backed graph library for knowledge graph traversals


# Development & testing
pytest==8.4.2
hypothesis==6.152.4  # Property-based testing

# ML experiment tracking
mlflow==3.15.0  # Model versioning and experiment tracking
